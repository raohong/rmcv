export type SpaceProps = {
  padding: {
    base: number;
    xs: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
  };
};

export const space: SpaceProps = {
  padding: {
    base: 4,
    xs: 8,
    sm: 12,
    md: 16,
    lg: 20,
    xl: 24,
  },
};
